const chai = require("chai");
const { prompt } = require("../index");

desccribe("prompt()", () => {
    context("entered input isn't a number", () => {
        it("should throw an TypeError", () => {

        });
    });
});